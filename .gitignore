# build intermediates
*.o
*.obj
/build/

# root build output (the distributable lives in release/)
/AngryMoz.exe
/mosquito.exe

# local machine state
mosquito.ini

# dev-only preview artifacts
preview_tray.*
