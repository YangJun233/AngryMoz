# build intermediates
*.o
*.obj
/build/

# root build output (the distributable lives in release/)
/AngryMoz.exe
/mosquito.exe

# packaged release zip (upload to GitHub Releases, not committed)
/AngryMoz-v*.zip

# local machine state
AngryMoz.ini
mosquito.ini

# dev-only preview artifacts
preview_tray.*
